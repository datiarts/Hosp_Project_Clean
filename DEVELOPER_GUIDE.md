# دليل المطور / Developer Guide

## 🏗️ هيكل المشروع / Project Structure

```
Hosp_Project_Clean/
│
├── main.py                      # نقطة دخول التطبيق / Application entry point
├── requirements.txt             # اعتماديات المشروع / Project dependencies
├── README.md                    # الوثائق الرئيسية / Main documentation
├── .gitignore                   # ملفات مستبعدة من Git / Git excluded files
│
├── config/                      # ملفات التكوين / Configuration files
│   ├── __init__.py
│   └── config.yaml             # إعدادات النظام / System settings
│
├── modules/                     # وحدات النظام / System modules
│   ├── __init__.py
│   ├── general_maintenance.py  # وحدة الصيانة العامة
│   ├── safety_security.py      # وحدة الأمن والسلامة
│   ├── medical_maintenance.py  # وحدة الصيانة الطبية
│   └── support_services.py     # وحدة الخدمات المساندة
│
├── dashboard/                   # لوحات المؤشرات / Dashboards
│   ├── __init__.py
│   └── kpi_dashboard.py        # لوحة مؤشرات الأداء
│
├── data/                        # قاعدة البيانات / Database files
│   └── (سيتم إنشاؤها تلقائياً / Created automatically)
│
└── reports/                     # التقارير المصدرة / Exported reports
    └── (سيتم إنشاؤها عند التصدير / Created on export)
```

## 🔧 إضافة ميزات جديدة / Adding New Features

### إضافة وحدة جديدة / Adding a New Module

1. أنشئ ملف Python جديد في مجلد `modules/`
2. استورد الوحدة في `main.py`
3. أضف خيار القائمة في `display_main_menu()`
4. أضف معالجة الاختيار في `run()`

مثال:
```python
# في modules/new_module.py
class NewModule:
    def __init__(self):
        pass
        
    def run(self):
        print("وحدة جديدة / New Module")
```

### إضافة مؤشر أداء جديد / Adding a New KPI

1. افتح `dashboard/kpi_dashboard.py`
2. أضف المؤشر الجديد في `__init__`
3. أنشئ دالة عرض جديدة
4. استدعِ الدالة في `display()`

## 📊 معايير سباهي المدعومة / Supported CBAHI Standards

### FMS.8 - السلامة من الحريق / Fire Safety
- FMS.8.1: تدريبات الطوارئ / Emergency Drills
- FMS.8.4: أنظمة الإنذار والإطفاء / Fire Alarm & Suppression

### FMS.3 - السلامة البيئية / Environmental Safety
- جولات السلامة الدورية / Regular Safety Rounds

### FMS.10 - إدارة الأجهزة الطبية / Medical Equipment Management
- FMS.10.2: جرد الأجهزة / Equipment Inventory
- FMS.10.3: الصيانة الوقائية / Preventive Maintenance

## 🎯 التطوير المستقبلي / Future Development

### المرحلة 1 (الحالية) - الهيكل الأساسي
- ✅ واجهة سطر الأوامر الأساسية
- ✅ الوحدات الأربع الرئيسية
- ✅ لوحة المؤشرات الأساسية

### المرحلة 2 - قاعدة البيانات
- [ ] تكامل SQLite
- [ ] حفظ واسترجاع البيانات
- [ ] تاريخ التغييرات

### المرحلة 3 - التقارير
- [ ] توليد تقارير Excel
- [ ] توليد تقارير PDF
- [ ] الرسوم البيانية

### المرحلة 4 - الواجهة الرسومية
- [ ] واجهة ويب بسيطة
- [ ] لوحات تحكم تفاعلية
- [ ] إشعارات تلقائية

## 💡 نصائح للمساهمين / Contributor Tips

1. **اتبع النمط الحالي**: استخدم نفس تنسيق القوائم والرسائل
2. **استخدم اللغتين**: اجعل جميع النصوص بالعربية والإنجليزية
3. **التعليقات**: اكتب تعليقات واضحة لكل دالة
4. **الاختبار**: اختبر جميع الميزات قبل الإضافة
5. **الامتثال**: تأكد من الالتزام بمعايير سباهي

## 📞 الدعم / Support

للمشاكل والاقتراحات، يرجى فتح Issue في GitHub.

---
تم التطوير بواسطة / Developed for: مستشفى رفحاء العام / Rafha General Hospital
